# Copia este archivo como .env solo para pruebas locales con `netlify dev`.
# En Netlify (producción) esta variable se configura en:
# Site settings → Environment variables → ANTHROPIC_API_KEY
# NUNCA subas un .env real con la key a git.
ANTHROPIC_API_KEY=
